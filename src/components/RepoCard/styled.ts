import styled from "@emotion/styled";

export const StyledRepoCard = styled.div`
  .repo-card {
    &__name {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
  }
`